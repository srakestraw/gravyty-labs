[build]
  publish = "out"
  command = "(npm install --legacy-peer-deps && echo '✓ npm install succeeded') || (echo 'ERROR: npm install failed' && exit 1); NODE_ENV=production npm run build:static || (echo 'ERROR: build:static failed' && exit 1)"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/guardrails"
  to = "/.netlify/functions/guardrails"
  status = 200
  force = true

[[redirects]]
  from = "/api/communication-config"
  to = "/.netlify/functions/communication-config"
  status = 200
  force = true




